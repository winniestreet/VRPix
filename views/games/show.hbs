<div class="container">
  <div class="col-md-10 game_show">
    <h1>{{game.title}}</h1>
    <p>{{game.description}}</p>
    <img class="game_image" src="{{game.imagePath}}"></img>
    
  </div>
</div>
